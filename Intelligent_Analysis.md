# Intelligent Analysis

```json
{
  "likely_target": "hired",
  "target_justification": "The 'hired' column is a binary variable (0/1) with 70.61% hired rate, representing the hiring decision outcome. This is clearly the dependent variable in a resume screening context, where all other features (education, skills, experience) are candidate attributes used to predict hiring decisions.",
  "problematic_columns": [
    "candidate_id",
    "resume_length_words"
  ],
  "insights": [
    "Class imbalance exists with 70.61% hired vs 29.39% not hired - may need stratified sampling or class weighting for modeling",
    "Data quality issue: resume_length_words has impossible negative minimum value (-79), indicating data entry errors that need investigation",
    "Experience distribution is highly right-skewed (skew=2.02) with max 23.55 years but mean only 1.5 years - suggests predominantly entry-level candidates with few senior outliers",
    "Research papers (mean=0.20) and hackathons (mean=0.50) are sparse features that most candidates lack, but could be strong differentiators for those who have them",
    "CGPA max of 11.23 exceeds typical 10-point scale, suggesting mixed grading systems across universities that may need normalization by university_tier"
  ],
  "analysis_code": "import matplotlib.pyplot as plt\nimport numpy as np\nimport pandas as pd\n\n# Create figure with subplots\nfig, axes = plt.subplots(2, 2, figsize=(14, 10))\nfig.suptitle('Resume Screening Analysis: Key Insights', fontsize=16, fontweight='bold')\n\n# 1. Target distribution\nax1 = axes[0, 0]\nhired_counts = df['hired'].value_counts()\nhired_pct = df['hired'].value_counts(normalize=True) * 100\nax1.bar(['Not Hired', 'Hired'], hired_counts.values, color=['#d62728', '#2ca02c'], alpha=0.7, edgecolor='black')\nfor i, (count, pct) in enumerate(zip(hired_counts.values, hired_pct.values)):\n    ax1.text(i, count, f'{count:,}\\n({pct:.1f}%)', ha='center', va='bottom', fontweight='bold')\nax1.set_ylabel('Number of Candidates', fontweight='bold')\nax1.set_title('Target Distribution: Hiring Outcome', fontweight='bold')\nax1.grid(axis='y', alpha=0.3)\n\n# 2. Statistics by education level (most interesting categorical)\nax2 = axes[0, 1]\nedu_stats = df.groupby('education_level')['hired'].agg(['mean', 'count']).sort_values('mean', ascending=False)\nedu_stats['hire_rate'] = edu_stats['mean'] * 100\ncolors_edu = ['#1f77b4', '#ff7f0e', '#2ca02c']\nbars = ax2.barh(edu_stats.index, edu_stats['hire_rate'], color=colors_edu, alpha=0.7, edgecolor='black')\nfor i, (idx, row) in enumerate(edu_stats.iterrows()):\n    ax2.text(row['hire_rate'], i, f\" {row['hire_rate']:.1f}% (n={int(row['count']):,})\", va='center', fontweight='bold')\nax2.set_xlabel('Hiring Rate (%)', fontweight='bold')\nax2.set_title('Hiring Rate by Education Level', fontweight='bold')\nax2.grid(axis='x', alpha=0.3)\n\n# 3. Correlation with target (numeric features only)\nax3 = axes[1, 0]\nnumeric_cols = df.select_dtypes(include=[np.number]).columns.tolist()\nnumeric_cols = [col for col in numeric_cols if col not in ['candidate_id', 'hired']]\ncorrelations = df[numeric_cols + ['hired']].corr()['hired'].drop('hired').sort_values(ascending=True)\ntop_corrs = pd.concat([correlations.head(5), correlations.tail(5)])\ncolors_corr = ['#d62728' if x < 0 else '#2ca02c' for x in top_corrs.values]\nax3.barh(range(len(top_corrs)), top_corrs.values, color=colors_corr, alpha=0.7, edgecolor='black')\nax3.set_yticks(range(len(top_corrs)))\nax3.set_yticklabels(top_corrs.index, fontsize=9)\nax3.set_xlabel('Correlation with Hired', fontweight='bold')\nax3.set_title('Top Features Correlated with Hiring Decision', fontweight='bold')\nax3.axvline(x=0, color='black', linestyle='-', linewidth=0.8)\nax3.grid(axis='x', alpha=0.3)\nfor i, v in enumerate(top_corrs.values):\n    ax3.text(v, i, f' {v:.3f}', va='center', fontsize=8, fontweight='bold')\n\n# 4. Experience vs Skills colored by hiring outcome\nax4 = axes[1, 1]\nhired_yes = df[df['hired'] == 1]\nhired_no = df[df['hired'] == 0]\nax4.scatter(hired_no['experience_years'], hired_no['skills_score'], alpha=0.3, s=10, c='#d62728', label='Not Hired')\nax4.scatter(hired_yes['experience_years'], hired_yes['skills_score'], alpha=0.3, s=10, c='#2ca02c', label='Hired')\nax4.set_xlabel('Years of Experience', fontweight='bold')\nax4.set_ylabel('Skills Score', fontweight='bold')\nax4.set_title('Experience vs Skills Score by Hiring Outcome', fontweight='bold')\nax4.legend(loc='lower right')\nax4.grid(alpha=0.3)\n\nplt.tight_layout()\nplt.savefig(os.path.join(_BASE_DIR, 'intelligent_analysis.png'), dpi=300, bbox_inches='tight')\nplt.close()\n\n# Print summary statistics\nprint('=== HIRING ANALYSIS SUMMARY ===')\nprint(f'\\nTotal Candidates: {len(df):,}')\nprint(f'Hired: {df[\"hired\"].sum():,} ({df[\"hired\"].mean()*100:.2f}%)')\nprint(f'Not Hired: {(df[\"hired\"]==0).sum():,} ({(1-df[\"hired\"].mean())*100:.2f}%)')\nprint('\\n=== HIRING RATE BY EDUCATION ===')\nprint(edu_stats[['hire_rate', 'count']].to_string())\nprint('\\n=== TOP CORRELATIONS WITH HIRING ===')\nprint(correlations.tail(10).to_string())",
  "feature_strategy": "Recommended feature engineering strategy: (1) Create composite scores combining related features (e.g., 'academic_strength' = normalized CGPA + education_level_encoded + university_tier_encoded; 'practical_experience' = internships + projects + hackathons; 'technical_depth' = programming_languages + certifications + skills_score). (2) Engineer interaction features like experience_per_age_ratio, projects_per_year, and skills_score * education_level to capture non-linear relationships. (3) Create binned categorical versions of continuous variables (e.g., experience_category: 'Entry'<1yr, 'Junior'1-3yrs, 'Senior'>3yrs) for interpretability. (4) Handle the CGPA scale inconsistency by normalizing within university_tier groups. (5) Fix resume_length_words negatives by taking absolute values or imputing. (6) Create binary flags for sparse features (has_research_papers, has_hackathons) and polynomial features for their counts when present. (7) Extract ratio features like certifications_per_programming_language to measure depth vs breadth of skills."
}
```